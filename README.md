# SimpleLocalDataBase
Simple local data base.
My first app on WindowsForms. Its just a little app with few windows. Data Base stores on project directory in users.json. Ah, and users have a "level", like access level i think. Users with 2 level can change other users levels. Maybe i update this later with more features.
